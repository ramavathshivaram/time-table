import React from "react";

type props = {
  title: string;
  timetableId: string;
};

const DesignerPanels = ({ title, timetableId }: props) => {
  return <div></div>;
};

export default DesignerPanels;
